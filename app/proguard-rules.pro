# Release minification is currently OFF (isMinifyEnabled = false). This file
# is the placeholder AGP expects in proguardFiles(). When/if R8 is turned on,
# add any keep rules here (e.g. for native JNI entry points).
